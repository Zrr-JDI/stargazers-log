# Test plan – PR #3 stars dashboard (http://localhost:8080/)

Evidence: script.js L18-49 (load/fallback, footer #source), L85-96 (language options "Lang (n)" sorted by count), L98-104 (stats), L106-120 (filter/sort), L122-149 (month grouping only when sort=starred), index.html toolbar (#search, #language, #sort). events.json: 6 repos, total 25053 stars, languages Python(2), JavaScript(1), HTML(1), Batchfile(1). GitHub API currently returns 403 from this box → fallback expected.

## T1 Initial load (1280 wide)
- 6 repo cards; month headers exactly "2024 11月", "2024 10月", "2024 09月" in that order.
- Stats: 6 / 25.1k / 4. Footer contains "数据来源：" with either GitHub API（实时） or events.json（缓存快照）.
- google/fonts card shows "★ 20.4k"; link href https://github.com/google/fonts.
- Console: no errors (a warn about fallback is acceptable).

## T2 Search
- Type "问卷" → exactly 2 cards (ZainCheung/wenjuanxin, QiuDi233/sojump-auto-fill), only a 2024 10月 header.
- Type "zzzzqq" → single "没有匹配的仓库。" item, no cards.
- Clear → 6 cards.

## T3 Language filter
- Options: 全部语言, Python (2), JavaScript (1), HTML (1), Batchfile (1) (Python first).
- Select Python → 2 cards (On-screen-real-time-translation, sojump-auto-fill). Reset to 全部语言 → 6.

## T4 Sort
- 最多 ⭐ → order google/fonts, BaiduYunVIP, wenjuanxin, xmind..., sojump, On-screen; NO month headers.
- 名称 A–Z → QiuDi233/..., YCYAX/..., ZainCheung/..., google/fonts, lpg-it/..., winturn/... (localeCompare; verify actual order is alphabetical case-insensitively); no month headers.
- 最近收藏 → month headers return.

## T5 Mobile (~400px window)
- Toolbar wraps (search above selects), stats wrap, no horizontal overflow (document.scrollingElement.scrollWidth <= innerWidth).
